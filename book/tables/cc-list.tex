\begin{longtable}{|>{\ttfamily}l|@{~}c@{}|L|>{\raggedright\arraybackslash}m{32ex}|}
\caption{Условия и~их связь с~флагами состояния~$flags$}\label{tab:cc-list} \\
% {|K{6em}|X|H|}
\hline
\normalfont\thead{CC} 
&
% \thead{Условие (флаги)}  
$
\begin{array}{c}
\text{\theadfont{Условие}}\\
\text{\theadfont{(флаги)}}\\
\end{array}
$
& %\theadfont Условие (sub src, dest или \mbox{cmp  src, dest})
% \thead{Условие (sub src, dest или \mbox{cmp  src, dest})}
\thead{Условие (арифметика)}
&
\theadfont Условие (sub src, dest или \mbox{cmp  src, dest})
\\\hline
e/z
&	
$ZF = 1$
&
% $dest - src = 0$ 
Результат равен нулю

\ethm{if zero}
&
$dest = src$ 
\att{$src  =  dest$}

\ethm{if equal}
\\\hline
ne/nz
&	
$ZF = 0$
&
% $dest - src \neq 0$ (то есть $dest \neq src$)
Результат не равен нулю

\ethm{if not zero}
&
$dest \neq src$
\att{$src \neq dest$}

\ethm{if not equal}
\\\hline
\begin{tabular}{@{}l@{}}
c/\\b/nae
\end{tabular}
% jc/b/nae
&	
$CF = 1$
&
Есть беззнаковое переполнение

% Результат не умещается в~$dest$ (полные $2^n$ бит) 

% Для сдвигов на один бит "--- вытесненный бит был единицей

\ethm{if carry }
&
$dest < src$ как беззнаковое 
\att{$src > dest$}
% "--- $dest$ ниже (below) $src$
% 
% (не выше и~не равно)

\ethm{if below / if not above or equal }
\\\hline
be/na
&
$
\left[
\begin{array}{@{\,}l}
CF = 1\\
ZF = 1
\end{array}
\right.
$
&
Есть беззнаковое переполнение
или~результат равен нулю
&
% не выше/ниже или равно 	
$dest \leqslant src$ как беззнаковое 
\att{$src \geqslant dest$}
% "--- $dest$ не выше $src$
% 
% (ниже или равно)

% \ethm{if below or equal}

\ethm{if below or equal / if not above}
\\\hline
\begin{tabular}{@{}l@{}}
nc/\\nb/ae
\end{tabular}
&	
$CF = 0$
&
Нет беззнакового переполнения

% Результат умещается в~$dest$ (полные $2^n$ бит)  

% Для сдвигов на один бит "--- вытесненный бит был нулём

\ethm{if not carry }
&
$dest \geqslant src$ как беззнаковое 
\att{$src \leqslant dest$}
% "--- $dest$ не ниже $src$ (выше или равно, above or equal)

\ethm{if not below / if above or equal }
\\\hline
nbe/a
&
$
\left\{
\begin{array}{@{\,}l}
CF = 0\\
ZF = 0
\end{array}
\right.
$
&
Нет беззнакового переполнения
и~результат \mbox{не равен нулю}
&
$dest > src$ как беззнаковое
\att{$src < dest$} 
% "--- $dest$ выше (above) $src$ 

% (не ниже и не равно)

\ethm{if not below or equal / if  above}
\\\hline

s &
$SF = 1$
&
\multicolumn{2}{E|}{
% Старший  бит результата (знаковый для знаковых чисел)
%  равен 1 
Старший (знаковый) бит результата равен 1 
\hfill
\ethm{if sign}
}% &
\\\hline

ns &
$SF = 0$
&
% Старший  бит результата (знаковый для знаковых чисел)
%  равен 0 
%  
% \ethm{if not sign }
% &
\multicolumn{2}{E|}{
% Старший  бит результата (знаковый для знаковых чисел)
%  равен 1 
Старший (знаковый) бит результата равен 0
\hfill
\ethm{if not sign}
}
\\\hline

o &
$OF = 1$
&
\multicolumn{2}{E|}{
Есть знаковое переполнение
\hfill
\ethm{if overflow}
}
% &
\\\hline

no &
$OF = 0$
&
\multicolumn{2}{E|}{
Нет знакового переполнения
\hfill
\ethm{if not overflow}
}
% &
\\\hline

l/nge
&
$SF\neq OF$
&
Результат %сложения/вычитания 
отрицателен

(знак равен 1 и~корректен 

или~равен 0, но некорректен)
&
$dest < src$ как знаковое
\att{$src > dest$}
% (не~больше и не~равно)
% 
% $dest - src < 0$ как знаковое %и~при этом помещается в~младшие $2^n - 1$ бит

\ethm{if less / if not greater or equal}
\\\hline

le/ng&
$
\left[
\begin{array}{@{\,}l}
SF\neq OF\\
ZF = 1
\end{array}
\right.
$&
Результат %сложения/вычитания 
% 
отрицателен или~\mbox{равен нулю}
&
$dest \leqslant src$ как знаковое
\att{$src \geqslant dest$}
% (меньше или равно/не больше)
% 
% $dest - src < 0$ как знаковое %и~при этом помещается в~младшие $2^n - 1$ бит

\ethm{if less or equal / if not greater}
\\\hline

nl/ge 
&
$SF = OF$
&
Результат %сложения/вычитания 
неотрицателен

(знак равен 0 и~корректен 

или равен~1, но~некорректен)
&
$dest \geqslant src$ как знаковое
\att{$src \leqslant dest$}

\ethm{if not less / if greater or equal}
\\\hline
nle/g
&
$
\left\{
\begin{array}{@{\,}l}
SF= OF\\
ZF = 0
\end{array}
\right.
$&
Результат 
% сложения/вычитания 
неотрицателен и~не~равен~нулю
&
$dest > src$ как знаковое
\att{$src < dest$}

\ethm{if not less or equal / if greater}
\\\hline

p/pe &
$PF = 1$
&
\multicolumn{2}{E|}{
Число единичных бит младшего байта результата чётно

\ethm{if parity / if parity even }
}
% &
\\\hline

np/po &
$PF = 0$
&
\multicolumn{2}{E|}{
Число единичных бит младшего байта результата нечётно

\ethm{if not parity / if parity odd }
}
% &
\\\hline
u &
$PF = 1$
&
\multicolumn{2}{E|}{
$src$ и~$dest$ несравнимы (только для \lstinline!fcmovCC!)
\hfill\ethm{if unordered}
}
\\\hline

nu &
$PF = 0$
&
\multicolumn{2}{E|}{
$src$ и~$dest$ сравнимы (только для \lstinline!fcmovCC!)
\hfill\ethm{if not unordered}
}
% &
\\\hline




\end{longtable}





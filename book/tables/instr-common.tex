\begin{longtable}{|>{\ttfamily}l|L|}
\caption{Основные общие команды}
\label{tab:asm-common-list}
\\
\hline
\normalfont\thead{Команда} & \thead{Действие}  %& \thead{Флаги} 
\\\hline
% nop
\adjustbox{valign=t}{\begin{tabular}{@{}l@{}}
nop
\\
nop srm
\end{tabular}}
& Ничего не делает \ethm{(no operation)}
\\\hline

mov src, dest & Присваивание 
% $src$ в~$dest$ %(\ethm{move;} размеры $src$ и~$dest$ совпадают).
$dest = src$
\ethm{(move)}
% 
% Операнды src и~dest могут быть переменными в~памяти (но не оба одновременно) 
% или регистрами,
% src "--- ещё и~константой.
% Операнд $src$ может быть константой, переменной в~памяти или регистром;
% $dest$ "--- переменной в~памяти или регистром
% & 
\\\hline
movabs imm64, dreg64 & В~64-битном режиме присваивание %$dreg64$ значения 
абсолютного 64-битного адреса $dreg64 = imm64$
% 
% Единственная команда с~64-битным непосредственным операндом
\\\hline

lea smem, dreg & Вычисление адреса $smem$ и~запись его в~$dreg$

$dreg = \&smem$
\ethm{(load effective address)}
% lea src, dest & Вычисляет адрес $src$ и~записывает его в~$dest$ (load effective address).
% 
% $src$ может быть только переменной в~памяти,
% $dest$ "--- только регистром
% & 
\\\hline
xchg srm, dest & Обмен значений $srm$ и~$dest$
\\\hline
\multicolumn{2}{|c|}{\textbf{Работа со стеком}}\\\hline
push src& Помещение $src$ в~стек (уменьшает указатель стека)
\\\hline
pop dest & Выталкивание значение из стека в~$dest$ (увеличивает указатель стека)
\\\hline
% \multicolumn{2}{|c|}{\textbf{Вызов и~возврат из функций}}\\\hline
% call proc
%  & Вызов подпрограммы "--- помещает в~стек адрес следующей инструкции (адрес возврата) и~переходит по адресу $proc$
% \\\hline
% ret [imm] & Возврат из подпрограммы "--- снимает со стека адрес возврата и~помещает его в~указатель команд.
% 
% Если указан параметр $imm$, снимает со стека ещё $imm$ байтов.
% \\\hline



\end{longtable}





\begin{longtable}{|l|L|}
\hline
\thead{ оманда} & \thead{ƒействие}  %& \thead{‘лаги} 
\\\hline
\lstinline!fabs!
& $st(0) := |st(0)|$
\\\hline
\lstinline!fsqrt!
&	
% вадратный корень \hfill
 $st(0) := \sqrt{st(0)}$
\\\hline
\lstinline!fptan!
&
% „астичный тангенс $st(0)$
% 
% % st(0) "--- угол в~радианах.
% % ¬ычисл€ютс€ $X$ и~$Y$, такие, что $X/Y = \tg(st(0))$ ()
% % $st(1) := X, st(0):= Y$, где 
% ¬ычисл€ет тангенс числа, наход€щегос€ в~регистре $st(0)$, замен€ет его на вычисленное значение и~затем помещает 1 в~стек, так что результат оказываетс€ в~$st(1)$, st(0) содержит~1.
% % 
% % ≈диница помещаетс€ в~стек дл€ того, чтобы можно было получить котангенс вызовом команды делени€ сразу после fptan и~дл€ совместимости с~ранними  модел€ми (до i386 в~$st(1)$ и~$st(0)$ помещались $X$ и~$Y$, такие, что $X/Y = \tg\big(st(0)\big)$)
% 
$\left\{\begin{array}{l}
st(0) := 1\\
st(1) := \tg\big(st(0)\big)\\
\end{array}\right.$
"--- частичный тангенс $st(0)$
\\\hline
\lstinline!fsincos!
&
% ¬ычисл€ет синус и~косинус числа, наход€щегос€ в~$st(0)$, помещает синус в~$st(0)$ и~затем помещает косинус в~стек (так что синус оказываетс€ в~$st(1)$, косинус "--- в~$st(0)$).
$\left\{\begin{array}{l}
st(0) := \cos\big(st(0)\big)\\
st(1) := \sin\big(st(0)\big)\\
\end{array}\right.$

\lstinline!fsincos! выполн€етс€ столько же времени, сколько \lstinline!fsin! или \lstinline!fcos! (и~вдвое меньше раздельного расчЄта синуса и~косинуса)
\\\hline
\lstinline!fsin!	& $st(0) := \sin\big(st(0)\big)$
\\\hline
\lstinline!fcos!	& $st(0) := \cos\big(st(0)\big)$
\\\hline
\lstinline!fscale!
& $st(0) := {st(0)}\cdot 2^{\lfloor st(1) \rfloor}$, ~~ $st(1)$ остаЄтс€ в~стеке
% «начение st(1) предварительно округл€етс€ в~сторону нул€ до целого числа. 
% не мен€етс€
\\\hline
\lstinline!f2xmi!
&$st(0) :=  2^{st(0)} -1$

% ¬озводит 2 в~степень, равную st(0), и~вычитает 1. –езультат сохран€етс€ в~st(0). 

«начение $st(0)$ должно лежать в~пределах от $-1$ до $+1$, иначе результат не определЄн.
\\\hline
\lstinline!fyl2x!
&
$\big[st(1)\to st(0)\big] := st(1)\cdot\log_2\big(st(0)\big)$, ~~ $st(0) \geqslant 0$

% ѕервоначальное значение st(0) должно быть неотрицательным. 

≈сли регистр $st(0)$ содержал ноль, результат (если ZM = 1) будет равен бесконечности со знаком, обратным $st(1)$.
\\\hline
\lstinline!fyl2xp1!
&
$\big[st(1)\to st(0)\big] := st(1)\cdot\log_2\big(st(0)+1\big)$,

% ѕервоначальное значение ST(0) должно быть в~пределах 
$st(0)$ от $-(1 - \frac{\sqrt{2}}{2})\approx -0,3$ до $(1 + \frac{\sqrt{2}}{2})\approx 1,7$, иначе результат не определЄн. 

 оманда \lstinline!fyl2xp1! дает б\'{о}льшую точность дл€ $st(0)$, близких к~нулю, чем \lstinline!fyl2x! дл€ суммы того же $st(0)$ и~$1$.
\\\hline
\lstinline!fprem1!
&
$\big[st(1)\to st(0)\big] := st(0) \Mod st(1)$ \hfill "--- частичный остаток %делени€ $st(0)$ на $st(1)$ 
по IEEE-754
% ¬ычисл€ет частичный остаток делени€ st(0) на st(1) по стандарту IEEE-754, помещает результат в~st(1) и~выталкивает st(0) из стека, так что после этой операции результат оказываетс€ в~st(0). 

ѕримен€етс€, в частности, дл€ уменьшени€ аргументов периодических функций: остаток от делени€ $3\cdot \pi +1.2$ на $\pi$ равен $1.2$.
\\\hline

\end{longtable}





\begin{longtable}{|l|L|}
% {|K{5em}|L|}
\hline
\thead{ оманда} & \thead{ƒействие}  %& \thead{‘лаги} 
\\\hline
fabs
& $st(0) := |st(0)|$
\\\hline
fsqrt
&	
% вадратный корень \hfill
 $st(0) := \sqrt{st(0)}$
\\\hline
fptan
&„астичный тангенс st(0)

% st(0) "--- угол в~радианах.
% ¬ычисл€ютс€ $X$ и~$Y$, такие, что $X/Y = \tg(st(0))$ ()
% $st(1) := X, st(0):= Y$, где 
¬ычисл€ет тангенс числа, наход€щегос€ в~регистре st(0), замен€ет его на вычисленное значение и~затем помещает 1 в~стек, так что результат оказываетс€ в~st(1), st(0) содержит~1.

≈диница помещаетс€ в~стек дл€ того, чтобы можно было получить котангенс вызовом команды fdivr сразу после fptan и~дл€ совместимости с~ранними  модел€ми (до i386 в~st(1) и~st(0) помещались $X$ и~$Y$, такие, что $X/Y = \tg\big(st(0)\big)$)
\\\hline
fsincos
&
¬ычисл€ет синус и~косинус числа, наход€щегос€ в~st(0), помещает синус в~st(0) и~затем помещает косинус в~стек (так что синус оказываетс€ в~st(1), косинус "--- в~st(0)).

fsincos выполн€етс€ столько же времени, сколько fsin или fcos (и~вдвое меньше раздельного расчЄта синуса и~косинуса)
\\\hline
fsin	& $st(0) := \sin\big(st(0)\big)$
\\\hline
fcos	& $st(0) := \cos\big(st(0)\big)$
\\\hline
fscale
& $st(0) := {st(0)}\cdot 2^{\lfloor st(1) \rfloor}$

«начение st(1) предварительно округл€етс€ в~сторону нул€ до целого числа. 
\\\hline
f2xmi
&$st(0) :=  2^{st(0)} -1$

% ¬озводит 2 в~степень, равную st(0), и~вычитает 1. –езультат сохран€етс€ в~st(0). 

«начение st(0) должно лежать в~пределах от $-1$ до $+1$, иначе результат не определЄн.
\\\hline
fyl2x
&
¬ычисл€ет $st(1)*\log_2\big(st(0)\big)$, помещает результат в~st(1) и~выталкивает st(0) из стека, так что после этой операции результат оказываетс€ в~st(0). 

ѕервоначальное значение st(0) должно быть неотрицательным. 

≈сли регистр st(0) содержал ноль, результат (если ZM = 1) будет равен бесконечности со знаком, обратным st(1).
\\\hline
fyl2xp1
&
¬ычисл€ет $st(1)*\log_2\big(st(0)+1\big)$, помещает результат в~st(1) и~выталкивает st(0) из стека, так что после этой операции результат оказываетс€ в~st(0). ѕервоначальное значение ST(0) должно быть в~пределах от $-(1 - \sqrt{2}/2)\approx -0,3$ до $(1 + \sqrt{2}/2)\approx 1,7$, иначе результат не определЄн. 

 оманда fyl2xp1 дает б\'{о}льшую точность дл€ st(0), близких к~нулю, чем fyl2x дл€ суммы того же st(0) и~1.
\\\hline

\end{longtable}




